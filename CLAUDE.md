# USDJPY マルチタイムフレーム戦略リサーチ

## このプロジェクトは何か
複数の自作 Pine 指標を題材に、指標の状況・位置関係から**再現性のある売買方式**を見つける研究。
バックテストの良し悪し判定ではなく、トレード1件ごとの分析が主眼。

## 最初に読むもの（権威ある設計書）
`docs/strategy-research-design.md` を必ず最初に読むこと。設計の全合意がここにある。
このファイルの方針に反する実装はしない。判断に迷ったらこの設計書が優先。

## 絶対に守る不変条件（設計書の要約。詳細は設計書参照）
1. **no-empty-result 原則**: 出力層に二値(採用/不採用)を持たせない。常に
   「順位付きの全景 + 確度ラベル + ベースレート併記」で出す。厳しい足切りで「該当なし」を返さない。
2. **ATR 正規化**: 全特徴量は生 pips でなく ATR 何本ぶんで持つ（足を跨いで同じ土俵に乗せる）。
3. **六足共通・係数可変**: 法則の形は六足共通、パラメータ影響度のみ足ごとに可変。
   検証の主役は期間分割より**足横断再現**（6足中5足で同方向に効くか）。
4. **R倍率で評価**: 勝率単体で判断しない。期待値で見る。1R = ATR(14)×1.5。
5. **リーク禁止**: 特徴量はその時点で過去データのみから計算可能であること。
   全ての発見は仮説として未使用データ/別足で再検証する。
6. **小さく始める**: 次元の呪い回避のため、整合性特徴量+核指標から開始し、効いたものだけ足す。

## レビュー時の必須チェック（lift が高いだけで採用しない）
- 経済的に筋が通るか（例: 強い速度→上昇 は自己相関の可能性。トレード可能な先行情報か）
- サンプル数は十分か（n が小さい葉は信用しない）
- 少しずらすと崩れるナイフエッジでないか
- リーク（先読み）していないか

## 対象
- 銘柄: USDJPY / エントリー足: 1分足 / 上位足(文脈): M15 H1 H4 D W
- データ源: Dukascopy（ティック→M1 を1ソースから取得し、上位5足は pandas でリサンプリング）
- D/W を作る前にタイムゾーンを固定する（Dukascopy は GMT 基準）

## リポジトリ構成
- `docs/`   設計書・Pine 原本
- `data/`   ヒストリカル（gitignore）
- `src/`    特徴量・分析・データ取得のコード
- `outputs/` 結果テーブル・図

## 進行順（最初のタスクから）
1. Dukascopy から USDJPY を小さい期間（まず1年）取得 → 六足にリサンプリング → ギャップ/TZ 検証
2. `src/physics_smoke_test.py` の `synth_prices()` を実データローダに差し替えて M1 で一周
3. 動いたら 六足展開 → 軸2（組み合わせ）→ Tier 合流 の順で拡張

## 開発スタイル
- 小さいコミット単位で進める。差分をレビューしてから受け入れる。
- 大きめのステップは先に計画を立ててから着手する。
